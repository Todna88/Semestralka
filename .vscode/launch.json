{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(gdb) loader example",
            "type": "cppdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": ["../src/vyroba.lp"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/build",
            "externalConsole": false,
            "MIMode": "gdb"
        }
    ]
}